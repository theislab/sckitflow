[build-system]
build-backend = "hatchling.build"
requires = [ "hatch-vcs", "hatchling" ]

[project]
name = "sckitflow"
description = "Flow Matching tool-box for Single Cell Data."
readme = "README.md"
license = { file = "LICENSE" }
maintainers = [
  { name = "Lorenzo Consoli", email = "lorenzo.consoli@helmholtz-munich.de" },
]
authors = [
  { name = "Lorenzo Consoli" },
]
requires-python = ">=3.12"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dynamic = [ "version" ]
dependencies = [
  "anndata>=0.11",
  "cloudpickle>=3",
  "numpy>=1.26",
  "pandas>=2",
  "scikit-learn>=1.4",
  "scipy>=1.12",
  "tqdm>=4.66",
]
# Everything an end user could want, minus the dev/doc/test tooling.
optional-dependencies.all = [
  "sckitflow[logging,torch]",
]

# Accelerator selectors for uv-managed development and CI environments.
optional-dependencies.cpu = [
  "torch>=2.2",
]
optional-dependencies.cu130 = [
  "torch>=2.2",
]

optional-dependencies.dev = [
  "pre-commit",
  "twine>=4.0.2",
]
optional-dependencies.doc = [
  "docutils>=0.8,!=0.18.*,!=0.19.*",
  "ipykernel",
  "ipython",
  "myst-nb>=1.1",
  "pandas",
  # Until pybtex >0.24.0 releases: https://bitbucket.org/pybtex-devs/pybtex/issues/169/
  "setuptools",
  "sphinx>=8.1",
  "sphinx-autodoc-typehints",
  "sphinx-book-theme>=1",
  "sphinx-copybutton",
  "sphinx-tabs",
  "sphinxcontrib-bibtex>=1",
  "sphinxext-opengraph",
]
# Imported lazily inside `sckitflow.trainer._callbacks.WandBLogger`.
optional-dependencies.logging = [
  "omegaconf",
  "wandb",
]
# Executes the notebooks under `docs/notebooks/` in CI.
optional-dependencies.notebooks = [
  "ipykernel",
  "matplotlib",
  "nbclient",
  "nbformat",
  "sckitflow[torch]",
]
optional-dependencies.test = [
  "coverage",
  "pytest",
  "pytest-cov",
  "pytest-split",             # shards the `slow` job by recorded duration (`.test_durations`)
  "pytest-xdist>=3",
  "scanpy>=1.10",             # reference implementation for the PCA transform tests only
  "sckitflow[logging,torch]", # TestWandBLogger imports omegaconf for real (wandb itself is mocked)
]
optional-dependencies.torch = [
  "pot>=0.9",
  "torch>=2.2",
  "torchdiffeq>=0.2.4",
  "torchmetrics>=1.4",
  "torchsde>=0.2.6",
]

# https://docs.pypi.org/project_metadata/#project-urls
urls.Documentation = "https://sckitflow.readthedocs.io/"
urls.Homepage = "https://github.com/theislab/sckitflow"
urls.Source = "https://github.com/theislab/sckitflow"

# Accelerator-specific sources are selected only when their matching extra is
# enabled. The generic `torch` extra remains accelerator-neutral.
[tool.uv]
conflicts = [
  [
    { extra = "cpu" },
    { extra = "cu130" },
  ],
]

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-cu130", extra = "cu130" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[tool.hatch.build.targets.wheel]
packages = [ "src/sckitflow" ]

[tool.hatch.version]
source = "vcs"

[tool.hatch.envs.default]
installer = "uv"
features = [ "dev" ]

[tool.hatch.envs.docs]
features = [ "doc" ]
scripts.build = "sphinx-build -M html docs docs/_build -W {args}"
scripts.open = "python -m webbrowser -t docs/_build/html/index.html"
scripts.clean = "git clean -fdX -- {args:docs}"

# Test the lowest and highest supported Python versions with normal deps
[[tool.hatch.envs.hatch-test.matrix]]
deps = [ "stable" ]
python = [ "3.12", "3.13" ]

# Test the newest supported Python version also with pre-release deps
[[tool.hatch.envs.hatch-test.matrix]]
deps = [ "pre" ]
python = [ "3.13" ]

[tool.hatch.envs.hatch-test]
features = [ "cpu", "test" ]

[tool.hatch.envs.hatch-test.overrides]
# If the matrix variable `deps` is set to "pre",
# set the environment variable `UV_PRERELEASE` to "allow".
matrix.deps.env-vars = [
  { key = "UV_PRERELEASE", value = "allow", if = [ "pre" ] },
]

[tool.ruff]
line-length = 120
src = [ "src" ]
extend-include = [ "*.ipynb" ]

format.docstring-code-format = true

lint.select = [
  "B",      # flake8-bugbear
  "BLE",    # flake8-blind-except
  "C4",     # flake8-comprehensions
  "D",      # pydocstyle
  "E",      # Error detected by Pycodestyle
  "F",      # Errors detected by Pyflakes
  "I",      # isort
  "RUF100", # Report unused noqa directives
  "TID",    # flake8-tidy-imports
  "UP",     # pyupgrade
  "W",      # Warning detected by Pycodestyle
]
lint.ignore = [
  "B008", # Errors from function calls in argument defaults. These are fine when the result is immutable.
  "D100", # Missing docstring in public module
  "D104", # Missing docstring in public package
  "D105", # __magic__ methods are often self-explanatory, allow missing docstrings
  "D107", # Missing docstring in __init__
  # Disable one in each pair of mutually incompatible rules
  "D203", # We don’t want a blank line before a class docstring
  "D213", # <> We want docstrings to start immediately after the opening triple quote
  "D400", # first line should end with a period [Bug: doesn’t work with single-line docstrings]
  "D401", # First line should be in imperative mood; try rephrasing
  "E501", # line too long -> we accept long comment lines; formatter gets rid of long code lines
  "E731", # Do not assign a lambda expression, use a def -> lambda expression assignments are convenient
  "E741", # allow I, O, l as variable names -> I is the identity matrix
]
lint.per-file-ignores."*/__init__.py" = [ "F401" ]
lint.per-file-ignores."docs/*" = [ "I" ]
lint.per-file-ignores."tests/*" = [ "D" ]
lint.pydocstyle.convention = "numpy"

[tool.pytest.ini_options]
testpaths = [ "tests" ]
xfail_strict = true
addopts = [
  "--import-mode=importlib", # allow using test files with same name
  "--strict-markers",
]
markers = [
  "slow: deselect with `-m 'not slow'` (large fixtures / long-running training loops)",
]

[tool.coverage.run]
source = [ "sckitflow" ]
# The test job runs under pytest-xdist, so each worker is a separate process and needs
# its own data file for `coverage combine` to pick it up.
parallel = true
omit = [
  "**/test_*.py",
]

[tool.cruft]
skip = [
  "tests",
  "src/**/__init__.py",
  "src/**/basic.py",
  "docs/api.md",
  "docs/changelog.md",
  "docs/references.bib",
  "docs/references.md",
  "docs/notebooks/example.ipynb",
]
